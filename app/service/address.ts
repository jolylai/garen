'use strict';

import { Service } from 'egg';

export default class AddressService extends Service {
  // find(params) {
  //   return this.ctx.model.Address.find(params);
  // }
  // create(params) {
  //   return this.ctx.model.Address.create(params);
  // }
  // update(params) {
  //   const doc = {
  //     name: params.name,
  //   };
  //   return this.ctx.model.User.updateOne({ _id: params._id }, doc);
  // }
  // deleteOne(params) {
  //   return this.ctx.model.Address.deleteOne(params);
  // }
}
